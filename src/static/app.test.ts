import { expect } from 'chai';
import './app.js';
import './user';
import './admin.js';
import './visualizer';

describe("API: get bookings", () => {
    it("should return response with bookings", () => {
        
    });
});
describe("API: post tables", () => {
    it("should return response with success", () => {
        
    });
});

describe("Admin to server: update roomplan", () => {
    it("should return response with success", () => {
        
    });
});
describe("Admin to server: add roomplan", () => {
    it("should return response with success", () => {
        
    });
});